# Instruction sheet

- **Job id**: 0001-3-board_api-test
- **Kind**: test
- **Parent Story**: 0001-3
- **Function name**: `board_api`
- **Folder**: `src/board_api/`
- **Files you may change**: `src/board_api/test_board_api.py` and nothing else. You never create or edit the code file.
- **Signature under test**: `board_api(name: str, query: dict, body: dict) -> object`
- **Inputs**: as stated. Import with `from board_api.board_api import board_api`.
- **Outputs**: per work/sheets/board_api-code.md
- **Errors**: KeyError for an unknown name
- **Allowed imports**: pytest, record_run, and the function under test. Nothing else.
- **Checklist items this job serves**: Story 0001-3 items 2, 3, 4, 5
- **Setup**: use the shared bd_repo fixture; create an intent with labels kind:intent,state:waiting,owner:board,care:high and a validation with a validates link to it.
- **Cases**, one test function each, exactly these names, nothing more:
  - `test_intents_has_care`: created intent with care:high -> 'intents' payload lists it with care 'high'
  - `test_item_fields`: 'item' with the intent id -> keys id, kind, title, owner, state, parent, sheet
  - `test_columns_shape`: 'columns' -> list whose first entry has keys name, role, wip, in_progress, items
  - `test_decide_roundtrip`: 'decide' with body {intent, 'yes', ''} -> payload state 'done'
  - `test_release_moves_ready`: 'release' with body {id: intent} on a waiting intent -> the intent's state label becomes state:ready
  - `test_release_rejects_non_intent`: 'release' on a story -> ValueError
  - `test_unknown_name_raises`: 'nope' -> KeyError
- **Checks to run before reporting**:
  - `ruff format src/board_api` then `ruff check src/board_api` (both clean)
  - `python -m pytest src/board_api -q`
- **Out of scope**: the code file, any other folder, any case not listed.
