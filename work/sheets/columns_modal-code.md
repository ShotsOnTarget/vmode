# Instruction sheet

- **Job id**: 0001-6-columns_modal-code
- **Kind**: code
- **Parent Story**: 0001-6
- **Function name**: `columns_modal`
- **Folder**: `src/columns_modal/`
- **Files you may change**: `src/columns_modal/columns_modal.py`, `src/columns_modal/columns_modal.md`
- **Signature**: `columns_modal() -> str`
- **Inputs**: none.
- **Outputs**: an HTML fragment string: a hidden modal (id 'modal') consisting of a fixed full-screen backdrop (rgba(0,0,0,.4)) with a centred white panel (max-width 640px, padding 20px, border-radius 8px), a Close button, a details area, a Yes button, a No button, a reason text input (hidden until No is pressed), and a script defining window.openItem(id): fetches GET /api/item?id=<id>, fills the details area with labelled rows for id, kind, title, owner, state, parent and the sheet as preformatted text in a scrolling box (max-height 50vh; white-space:pre-wrap), shows Yes/No only when kind is intent or proposal and state is checking, Yes posts JSON {intent: id, decision: 'yes', reason: ''} to /api/decide, No reveals the reason input and posts with decision 'no' once it is non-empty, and after either the modal closes and location.reload() runs; Close, clicking the backdrop, and the Escape key close the modal. No alert, confirm or prompt; no external resources.
- **Errors**: none.
- **Allowed imports**: none. Nothing else.
- **Checklist items this job serves**: Story 0001-6 items 1, 2, 3
- **How**: One triple-quoted string with dense HTML, CSS and JS lines; the Python file stays under 50 lines. If it cannot fit, report blocked.
- **Checks to run before reporting**:
  - `ruff format src/columns_modal` then `ruff check src/columns_modal` (both clean; a noqa comment fails the shape check)
  - `python tools/lint.py`   (no findings for your folder)
  - `python -c "import ast,sys; t=ast.parse(open('src/columns_modal/columns_modal.py').read()); print(sum(isinstance(x,ast.FunctionDef) and not x.name.startswith('_') for x in t.body))"`   (must print 1)
- **Note file** `src/columns_modal/columns_modal.md` has exactly these six lines: purpose, signature, inputs, outputs, side effects, work item id 0001-6-columns_modal-code.
- **Out of scope**: tests, any other folder, any import not listed. If the formatted file exceeds 50 lines, report blocked.
