# Instruction sheet

- **Job id**: 0001-6-board_columns-code
- **Kind**: code
- **Parent Story**: 0001-6
- **Function name**: `board_columns`
- **Folder**: `src/board_columns/`
- **Files you may change**: `src/board_columns/board_columns.py`, `src/board_columns/board_columns.md`
- **Signature**: `board_columns(graph: dict, labels: dict, config: dict) -> list[dict]`
- **Inputs**: graph is the dict documented in work/sheets/record_graph-code.md; labels is id -> label list; config is from board_config
- **Outputs**: one dict per column in config order: {'name', 'role', 'wip', 'in_progress': count of items placed in that column whose state is in_progress, 'items': [{'id','kind','title','state'}] in id order}. Items whose column_of is None are omitted.
- **Errors**: none.
- **Allowed imports**: from column_of.column_of import column_of. Nothing else.
- **Checklist items this job serves**: Story 0001-6 items 1, 2
- **How**: Pure. Place every item once with column_of(item, labels.get(id, []), config), then build the list. Under 35 lines.
- **Checks to run before reporting**:
  - `ruff format src/board_columns` then `ruff check src/board_columns` (both clean; a noqa comment fails the shape check)
  - `python tools/lint.py`   (no findings for your folder)
  - `python -c "import ast,sys; t=ast.parse(open('src/board_columns/board_columns.py').read()); print(sum(isinstance(x,ast.FunctionDef) and not x.name.startswith('_') for x in t.body))"`   (must print 1)
- **Note file** `src/board_columns/board_columns.md` has exactly these six lines: purpose, signature, inputs, outputs, side effects, work item id 0001-6-board_columns-code.
- **Out of scope**: tests, any other folder, any import not listed. Never hand-pack lines. If the formatted file exceeds 50 lines, report blocked.
