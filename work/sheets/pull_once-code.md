# Instruction sheet

- **Job id**: 0003-4-pull_once-code
- **Kind**: code
- **Parent Story**: 0003-4
- **Function name**: `pull_once`
- **Folder**: `src/pull_once/`
- **Files you may change**: `src/pull_once/pull_once.py`, `src/pull_once/pull_once.md`
- **Signature**: `pull_once(role: str, config_path: str, invoke) -> list[str]`
- **Inputs**: role: a role name in the config. config_path. invoke: a callable (item: dict, column: str) -> dict with keys tokens, seconds, report; it may raise.
- **Outputs**: ids claimed this pass. For each column in the config whose role matches, in config order: headroom = min(wip_headroom(column), global headroom) where global headroom = limits.max_parallel_model_runs minus the count of in_progress items across all columns whose tier is not 'none' or 'human'; take that many from column_items; for each: claim_item(id, actor) where actor = role + '-' + str(os.getpid()); then usage = invoke(item, column); record_add_note(id, 'usage: ' + json.dumps(usage)); record_set_state(id, 'checking'); on any exception from invoke: record_set_state(id, 'ready') and record_add_note(id, 'release: ' + str(exc)[:200]).
- **Errors**: ValueError if role has no column. RecordError propagates.
- **Allowed imports**: json, os, from board_config.board_config import board_config; from record_graph.record_graph import record_graph; from record_labels.record_labels import record_labels; from column_items.column_items import column_items; from wip_headroom.wip_headroom import wip_headroom; from claim_item.claim_item import claim_item; from record_add_note.record_add_note import record_add_note; from record_set_state.record_set_state import record_set_state. Nothing else.
- **Checklist items this job serves**: Story 0003-4 items 1, 2, 3, 7
- **How**: Config is re-read on every call. One private helper for global headroom and one for the claim-invoke-move sequence. Under 50 lines; if not possible report blocked. Any output shape you rely on from bd must be captured from a real run first, and the command named in the note file.
- **Checks to run before reporting**:
  - `ruff format src/pull_once` then `ruff check src/pull_once` (both clean; a noqa comment fails the shape check)
  - `python tools/lint.py`   (no findings for your folder)
  - `python -c "import ast,sys; t=ast.parse(open('src/pull_once/pull_once.py').read()); print(sum(isinstance(x,ast.FunctionDef) and not x.name.startswith('_') for x in t.body))"`   (must print 1)
- **Note file** `src/pull_once/pull_once.md` has exactly these six lines: purpose, signature, inputs, outputs, side effects, work item id 0003-4-pull_once-code.
- **Out of scope**: tests, any other folder, any import not listed. Never hand-pack lines. If the formatted file exceeds 50 lines, report blocked.
