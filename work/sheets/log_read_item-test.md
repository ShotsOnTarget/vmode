# Instruction sheet

- **Job id**: 0001-5-log_read_item-test
- **Kind**: test
- **Parent Story**: 0001-5
- **Function name**: `log_read_item`
- **Folder**: `src/log_read_item/`
- **Files you may change**: `src/log_read_item/test_log_read_item.py`
- **Signature under test**: `log_read_item(path: str, item_id: str) -> list[dict]`
- **Inputs**: as stated in the signature. Import with `from log_read_item.log_read_item import log_read_item` (and RecordError where needed, from `record_run.record_run`).
- **Outputs**: every entry whose item equals item_id, in file order. [] if file missing.
- **Errors**: raise ValueError if any line is not valid JSON.
- **Allowed imports**: pytest, json, os, subprocess, shutil, pathlib, and the function under test. Nothing else.
- **Checklist items this job serves**: Story 0001-5 items 3
- **Setup**: record_* tests run `bd init --prefix vm --non-interactive` in `tmp_path` in a fixture and `monkeypatch.chdir(tmp_path)`. log_* tests use `tmp_path / 'log.jsonl'`.
- **Cases**, one test function each, exactly these names, nothing more:
  - `test_returns_matching_in_order`: three lines for item A interleaved with two for B; read A returns the three in order
  - `test_missing_file_returns_empty`: nonexistent path returns []
  - `test_bad_line_raises`: a line of plain text raises ValueError
  - `test_file_unchanged_after_read`: file bytes identical before and after
- **Checks to run before reporting**:
  - `python -m pytest src/log_read_item -q`
- **Out of scope**: the code file, any other folder, any case not listed, any assertion on internals.
