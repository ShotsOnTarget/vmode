# Instruction sheet

- **Job id**: 0003-2-record_labels-code
- **Kind**: code
- **Parent Story**: 0003-2
- **Function name**: `record_labels`
- **Folder**: `src/record_labels/`
- **Files you may change**: `src/record_labels/record_labels.py`, `src/record_labels/record_labels.md`
- **Signature**: `record_labels() -> dict[str, list[str]]`
- **Inputs**: none. Reads the record once with record_run(['list', '--all']).
- **Outputs**: id -> that item's labels list, for every item.
- **Errors**: RecordError propagates.
- **Allowed imports**: from record_run.record_run import record_run, RecordError. Nothing else.
- **Checklist items this job serves**: Story 0003-2 items 3, 4
- **How**: One call, one comprehension. Under 15 lines. Any output shape you rely on from bd must be captured from a real run first, and the command named in the note file.
- **Checks to run before reporting**:
  - `ruff format src/record_labels` then `ruff check src/record_labels` (both clean; a noqa comment fails the shape check)
  - `python tools/lint.py`   (no findings for your folder)
  - `python -c "import ast,sys; t=ast.parse(open('src/record_labels/record_labels.py').read()); print(sum(isinstance(x,ast.FunctionDef) and not x.name.startswith('_') for x in t.body))"`   (must print 1)
- **Note file** `src/record_labels/record_labels.md` has exactly these six lines: purpose, signature, inputs, outputs, side effects, work item id 0003-2-record_labels-code.
- **Out of scope**: tests, any other folder, any import not listed. Never hand-pack lines. If the formatted file exceeds 50 lines, report blocked.
