# Instruction sheet

- **Job id**: 0003-2-column_items-code
- **Kind**: code
- **Parent Story**: 0003-2
- **Function name**: `column_items`
- **Folder**: `src/column_items/`
- **Files you may change**: `src/column_items/column_items.py`, `src/column_items/column_items.md`
- **Signature**: `column_items(name: str, graph: dict, labels: dict[str, list[str]], config: dict) -> list[dict]`
- **Inputs**: name: a column name in config. graph is the dict documented in work/sheets/record_graph-code.md. labels: id -> label list. config as above.
- **Outputs**: items whose column_of(...) == name, excluding any item with a needs entry whose target state is not done, ordered by id.
- **Errors**: ValueError if name is not a column.
- **Allowed imports**: from column_of.column_of import column_of. Nothing else.
- **Checklist items this job serves**: Story 0003-2 items 4
- **How**: Pure. Under 25 lines.
- **Checks to run before reporting**:
  - `ruff format src/column_items` then `ruff check src/column_items` (both clean; a noqa comment fails the shape check)
  - `python tools/lint.py`   (no findings for your folder)
  - `python -c "import ast,sys; t=ast.parse(open('src/column_items/column_items.py').read()); print(sum(isinstance(x,ast.FunctionDef) and not x.name.startswith('_') for x in t.body))"`   (must print 1)
- **Note file** `src/column_items/column_items.md` has exactly these six lines: purpose, signature, inputs, outputs, side effects, work item id 0003-2-column_items-code.
- **Out of scope**: tests, any other folder, any import not listed. Never hand-pack lines. If the formatted file exceeds 50 lines, report blocked.
