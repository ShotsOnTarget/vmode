# Instruction sheet

- **Job id**: 0003-1-gate_ready-test
- **Kind**: test
- **Parent Story**: 0003-1
- **Function name**: `gate_ready`
- **Folder**: `src/gate_ready/`
- **Files you may change**: `src/gate_ready/test_gate_ready.py`
- **Signature under test**: `gate_ready(story_id: str, graph: dict, sheets: dict[str, str]) -> list[str]`
- **Inputs**: as stated. Import with `from gate_ready.gate_ready import gate_ready`.
- **Outputs**: rules broken, in this fixed order, empty if none: 'no_intent' (walking parent from the story never reaches an item of kind intent), 'code_without_test' (a code job under the story with no test item whose checks contains it), 'test_without_code' (a test item whose checks target is not a code job under the story), 'sheet_missing' (a code or test job under the story whose sheet text is empty or absent), 'orphan' (find_orphans(graph) reports any item under the story).
- **Errors**: ValueError if story_id not in graph or its kind is not story.
- **Allowed imports**: pytest, json, and the function under test. Nothing else. No record server is needed.
- **Checklist items this job serves**: Story 0003-1 items 1, 5
- **Setup**: hand-build graphs, strings and log files under tmp_path. Test files have no line limit but must be formatter and linter clean.
- **Cases**, one test function each, exactly these names, nothing more:
  - `test_code_without_test`: story with one code job and no test -> ['code_without_test', 'sheet_missing'] or at least contains 'code_without_test'
  - `test_no_intent`: story whose parent is None -> contains 'no_intent'
  - `test_sheet_missing`: complete pair but sheets lacks the test id -> contains 'sheet_missing'
  - `test_clean_passes`: intent, story, code, test(checks code), verification(checks story), validation(checks intent), sheets for both jobs -> []
  - `test_not_a_story_raises`: passing a code id raises ValueError
- **Checks to run before reporting**:
  - `ruff format src/gate_ready` then `ruff check src/gate_ready` (both clean)
  - `python -m pytest src/gate_ready -q`
- **Out of scope**: the code file, any other folder, any case not listed, any assertion on internals.
