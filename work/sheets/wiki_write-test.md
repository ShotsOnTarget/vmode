# Instruction sheet

- **Job id**: 0002-2-wiki_write-test
- **Kind**: test
- **Parent Story**: 0002-2
- **Function name**: `wiki_write`
- **Folder**: `src/wiki_write/`
- **Files you may change**: `src/wiki_write/test_wiki_write.py`
- **Signature under test**: `wiki_write(root: str, page: dict) -> str`
- **Inputs**: as stated. Import with `from wiki_write.wiki_write import wiki_write`.
- **Outputs**: path of the written file `<root>/<id>.md`, overwriting if present.
- **Errors**: ValueError if any field is missing or extra, if evidence is empty, or if any evidence string contains 'src/' (pages never reference code).
- **Allowed imports**: pytest, json, os, datetime, and the function under test plus wiki_write/wiki_read where the case needs a page on disk. Nothing else. No record server is needed for these tests.
- **Checklist items this job serves**: Story 0002-2 items 1, 2
- **Setup**: use `tmp_path` as root or log path. For wiki pages you may write files directly in the documented format (see work/sheets/wiki_write-code.md How line) or via wiki_write.
- **Cases**, one test function each, exactly these names, nothing more:
  - `test_write_creates_file`: file exists at returned path and starts with '---'
  - `test_missing_field_rejected`: page without 'fix' raises ValueError and no file is written
  - `test_code_reference_rejected`: evidence ['src/x/x.py'] raises ValueError
  - `test_overwrite_replaces`: write twice with different title; file contains the second title only
- **Checks to run before reporting**:
  - `python -m pytest src/wiki_write -q`
  - `wc -l src/wiki_write/test_wiki_write.py   (must print 50 or less)`
- **Out of scope**: the code file, any other folder, any case not listed, any assertion on internals.
