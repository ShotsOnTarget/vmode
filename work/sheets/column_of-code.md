# Instruction sheet

- **Job id**: 0003-2-column_of-code
- **Kind**: code
- **Parent Story**: 0003-2
- **Function name**: `column_of`
- **Folder**: `src/column_of/`
- **Files you may change**: `src/column_of/column_of.py`, `src/column_of/column_of.md`
- **Signature**: `column_of(item: dict, labels: list[str], config: dict) -> str | None`
- **Inputs**: item: one value from the graph (graph is the dict documented in work/sheets/record_graph-code.md). labels: that item's label list from the record (the graph does not carry labels). config is the dict returned by board_config: {'columns': {name: {'kinds': [...], 'states': [...], 'role': str, 'tier': str, 'wip': int, 'poll_seconds': int, 'labels_absent': [...] optional}}, 'limits': {'max_parallel_model_runs': int, 'claim_timeout_seconds': int}}.
- **Outputs**: the single column name whose kinds contains item['kind'], states contains item['state'], and none of whose labels_absent entries is in labels. None if no column matches.
- **Errors**: ValueError if more than one column matches (should be impossible after board_config validation, but checked).
- **Allowed imports**: none. Nothing else.
- **Checklist items this job serves**: Story 0003-2 items 3
- **How**: Pure. Under 25 lines.
- **Checks to run before reporting**:
  - `ruff format src/column_of` then `ruff check src/column_of` (both clean; a noqa comment fails the shape check)
  - `python tools/lint.py`   (no findings for your folder)
  - `python -c "import ast,sys; t=ast.parse(open('src/column_of/column_of.py').read()); print(sum(isinstance(x,ast.FunctionDef) and not x.name.startswith('_') for x in t.body))"`   (must print 1)
- **Note file** `src/column_of/column_of.md` has exactly these six lines: purpose, signature, inputs, outputs, side effects, work item id 0003-2-column_of-code.
- **Out of scope**: tests, any other folder, any import not listed. Never hand-pack lines. If the formatted file exceeds 50 lines, report blocked.
