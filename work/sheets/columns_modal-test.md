# Instruction sheet

- **Job id**: 0001-6-columns_modal-test
- **Kind**: test
- **Parent Story**: 0001-6
- **Function name**: `columns_modal`
- **Folder**: `src/columns_modal/`
- **Files you may change**: `src/columns_modal/test_columns_modal.py` and nothing else. You never create or edit the code file.
- **Signature under test**: `columns_modal() -> str`
- **Inputs**: as stated. Import with `from columns_modal.columns_modal import columns_modal`.
- **Outputs**: an HTML fragment string
- **Errors**: none
- **Allowed imports**: pytest and the function under test. Nothing else.
- **Checklist items this job serves**: Story 0001-6 items 1, 2, 3
- **Setup**: none needed.
- **Cases**, one test function each, exactly these names, nothing more:
  - `test_has_endpoints`: contains '/api/item' and '/api/decide'
  - `test_has_open_function`: contains 'openItem'
  - `test_has_escape_and_close`: contains 'Escape' and 'Close'
  - `test_no_dialogs_no_external`: contains none of 'alert(', 'confirm(', 'prompt(', 'http://', 'https://'
- **Checks to run before reporting**:
  - `ruff format src/columns_modal` then `ruff check src/columns_modal` (both clean)
  - `python -m pytest src/columns_modal -q`
- **Out of scope**: the code file, any other folder, any case not listed.
