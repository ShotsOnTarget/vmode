# Instruction sheet

- **Job id**: 0003-2-column_items-test
- **Kind**: test
- **Parent Story**: 0003-2
- **Function name**: `column_items`
- **Folder**: `src/column_items/`
- **Files you may change**: `src/column_items/test_column_items.py`
- **Signature under test**: `column_items(name: str, graph: dict, labels: dict[str, list[str]], config: dict) -> list[dict]`
- **Inputs**: as stated. Import with `from column_items.column_items import column_items`.
- **Outputs**: items whose column_of(...) == name, excluding any item with a needs entry whose target state is not done, ordered by id.
- **Errors**: ValueError if name is not a column.
- **Allowed imports**: pytest, tomllib, and the functions under test plus board_config. Nothing else.
- **Checklist items this job serves**: Story 0003-2 items 4
- **Setup**: hand-build graphs, label dicts and TOML files under tmp_path; use board_config('roles/board.toml') for the shipped config.
- **Cases**, one test function each, exactly these names, nothing more:
  - `test_lists_matching`: two ready code jobs and one ready test -> column 'build' returns the two code ids in id order
  - `test_excludes_blocked_by_needs`: a ready code job with needs ['x'] where graph['x']['state'] == 'waiting' is excluded
  - `test_includes_when_needs_done`: same but state done -> included
  - `test_unknown_column_raises`: ValueError
- **Checks to run before reporting**:
  - `ruff format src/column_items` then `ruff check src/column_items` (both clean)
  - `python -m pytest src/column_items -q`
- **Out of scope**: the code file, any other folder, any case not listed.
