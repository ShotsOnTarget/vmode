# Instruction sheet

- **Job id**: 0003-4-commit_job-test
- **Kind**: test
- **Parent Story**: 0003-4
- **Function name**: `commit_job`
- **Folder**: `src/commit_job/`
- **Files you may change**: `src/commit_job/test_commit_job.py` and nothing else. You never create or edit the code file.
- **Signature under test**: `commit_job(job_id: str, folder: str, repo: str = ".") -> str | None`
- **Inputs**: as stated. Import with `from commit_job.commit_job import commit_job`.
- **Outputs**: commit hash or None
- **Errors**: RuntimeError on git failure
- **Allowed imports**: pytest, subprocess, pathlib, and the function under test. Nothing else.
- **Checklist items this job serves**: Story 0003-4 items 4, 5
- **Setup**: create a temporary git repository under tmp_path with `git init -q`, `git config user.email t@t`, `git config user.name t`, and a folder src/x with one file. Never touch this repository's own git state.
- **Cases**, one test function each, exactly these names, nothing more:
  - `test_commits_only_folder`: two changed folders src/x and src/y; commit_job('vm-1', 'x', repo) returns a hash and `git show --stat --format=` lists only src/x files; src/y remains uncommitted
  - `test_message_names_job`: the commit message contains 'Job: vm-1' and starts with 'x: job vm-1'
  - `test_nothing_to_commit_none`: calling again with no changes returns None
  - `test_git_failure_raises`: repo path that is not a git repository raises RuntimeError
- **Checks to run before reporting**:
  - `ruff format src/commit_job` then `ruff check src/commit_job` (both clean)
  - `python -m pytest src/commit_job -q`
- **Out of scope**: the code file, any other folder, any case not listed.
