# Instruction sheet

- **Job id**: 0003-4-adapter_command-test
- **Kind**: test
- **Parent Story**: 0003-4
- **Function name**: `adapter_command`
- **Folder**: `src/adapter_command/`
- **Files you may change**: `src/adapter_command/test_adapter_command.py` and nothing else. You never create or edit the code file.
- **Signature under test**: `adapter_command(item: dict, column: str, roots: dict) -> list[str]`
- **Inputs**: as stated. Import with `from adapter_command.adapter_command import adapter_command`.
- **Outputs**: argv list as described in work/sheets/adapter_command-code.md
- **Errors**: RuntimeError when claude is not on PATH
- **Allowed imports**: pytest, json, os, shutil, tomllib, record_run, and the function under test. Nothing else.
- **Checklist items this job serves**: Story 0003-4 items 8
- **Setup**: roots = {'board': 'roles/board.toml', 'manifest': 'roles/manifest.json', 'root': '.'}; use monkeypatch on shutil.which; no record needed.
- **Cases**, one test function each, exactly these names, nothing more:
  - `test_exe_resolved`: with monkeypatch.setattr(shutil, 'which', lambda n: 'C:/x/claude.cmd') the first element is 'C:/x/claude.cmd'
  - `test_missing_exe_raises`: which returning None -> RuntimeError
  - `test_model_from_manifest`: column 'build' with the shipped files -> '--model' followed by the manifest's cheap model name without provider prefix
  - `test_prompt_mentions_item_and_role`: prompt contains the item id and 'You are the builder'
  - `test_test_kind_wording`: kind test -> prompt contains 'never create or edit the code file'
- **Checks to run before reporting**:
  - `ruff format src/adapter_command` then `ruff check src/adapter_command` (both clean)
  - `python -m pytest src/adapter_command -q`
- **Out of scope**: the code file, any other folder, any case not listed.
