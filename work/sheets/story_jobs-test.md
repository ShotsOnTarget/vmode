# Instruction sheet

- **Job id**: 0003-1-story_jobs-test
- **Kind**: test
- **Parent Story**: 0003-1
- **Function name**: `story_jobs`
- **Folder**: `src/story_jobs/`
- **Files you may change**: `src/story_jobs/test_story_jobs.py`
- **Signature under test**: `story_jobs(story_id: str, graph: dict) -> dict`
- **Inputs**: as stated. Import with `from story_jobs.story_jobs import story_jobs`.
- **Outputs**: as in work/sheets/story_jobs-code.md Outputs line
- **Errors**: ValueError if story_id not in graph
- **Allowed imports**: pytest, json, and the function under test. Nothing else.
- **Checklist items this job serves**: Story 0003-1 items 1, 5
- **Setup**: hand-build graphs and log files under tmp_path.
- **Cases**, one test function each, exactly these names, nothing more:
  - `test_has_intent_true`: intent > story -> has_intent True
  - `test_has_intent_false`: story with parent None -> False
  - `test_code_and_tests`: story with code c1 and test t1 (checks [c1]) -> code ['c1'], tests {'c1': ['t1']}
  - `test_code_without_test_empty_list`: code c1 and no test -> tests {'c1': []}
  - `test_unknown_raises`: ValueError
- **Checks to run before reporting**:
  - `ruff format src/story_jobs` then `ruff check src/story_jobs` (both clean)
  - `python -m pytest src/story_jobs -q`
- **Out of scope**: the code file, any other folder, any case not listed.
