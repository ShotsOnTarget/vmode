# Instruction sheet

- **Job id**: 0003-4-puller-test
- **Kind**: test
- **Parent Story**: 0003-4
- **Function name**: `puller`
- **Folder**: `src/puller/`
- **Files you may change**: `src/puller/test_puller.py` and nothing else. You never create or edit the code file.
- **Signature under test**: `puller(role: str, config_path: str, invoke, stop_file: str, once=None) -> int`
- **Inputs**: as stated. Import with `from puller.puller import puller`.
- **Outputs**: the number of passes made. Loop: call once(role, config_path, invoke); sleep the smallest poll_seconds among the role's columns (at least 1); repeat until stop_file exists; check the stop file both before and after sleeping.
- **Errors**: ValueError if the role has no column with poll_seconds > 0 (humans are not polled).
- **Allowed imports**: pytest, json, os, shutil, pathlib, record_run, record_show_item, log_read_item, and the function under test. Nothing else.
- **Checklist items this job serves**: Story 0003-4 items 8
- **Setup**: hand-build inputs under tmp_path; a fake once is a plain function.
- **Cases**, one test function each, exactly these names, nothing more:
  - `test_stops_on_file`: create the stop file before calling -> returns 0 without calling once
  - `test_passes_counted`: a fake once that creates the stop file on its second call, poll 1 second -> returns 2
  - `test_human_role_raises`: role 'board' -> ValueError
- **Checks to run before reporting**:
  - `ruff format src/puller` then `ruff check src/puller` (both clean)
  - `python -m pytest src/puller -q`
- **Out of scope**: the code file, any other folder, any case not listed.
