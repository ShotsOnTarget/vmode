# Instruction sheet

- **Job id**: 0001-3-board_rollup-test
- **Kind**: test
- **Parent Story**: 0001-3
- **Function name**: `board_rollup`
- **Folder**: `src/board_rollup/`
- **Files you may change**: `src/board_rollup/test_board_rollup.py`
- **Signature under test**: `board_rollup(graph: dict[str, dict], care: dict[str, str]) -> list[dict]`
- **Inputs**: as stated. Import with `from board_rollup.board_rollup import board_rollup`.
- **Outputs**: one dict per intent, id order: {'id','title','state','care','stories_total','stories_done'}. stories are items of kind story whose parent is the intent; done means state == 'done'.
- **Errors**: none. Intent missing from care gets care ''.
- **Allowed imports**: pytest, json, threading, socket, urllib.request, urllib.error, record_run, record_graph, and the function under test. Nothing else.
- **Checklist items this job serves**: Story 0001-3 items 2
- **Setup**: use the shared fixture `bd_repo` from `src/conftest.py` where the record is needed. Build items with record_run(['create', ...]) using the flags in roles/work-record/CONVENTIONS.md (always --no-inherit-labels; right-side kinds get a validates dep, never --parent; intents get a care:high or care:low label). For pure functions hand-build a graph dict in the shape documented in work/sheets/record_graph-code.md.
- **Cases**, one test function each, exactly these names, nothing more:
  - `test_counts_stories`: hand-built graph, one intent with three stories, two done -> stories_total 3, stories_done 2
  - `test_care_passthrough`: care {'i1':'high'} -> row care 'high'; missing -> ''
  - `test_no_intents_empty`: graph with only a story -> []
  - `test_id_order`: two intents 'vm-b','vm-a' -> rows in order vm-a, vm-b
- **Checks to run before reporting**:
  - `python -m pytest src/board_rollup -q`
- **Out of scope**: the code file, any other folder, any case not listed, any assertion on internals.
