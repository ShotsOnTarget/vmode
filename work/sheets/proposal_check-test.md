# Instruction sheet

- **Job id**: 0002-4-proposal_check-test
- **Kind**: test
- **Parent Story**: 0002-4
- **Function name**: `proposal_check`
- **Folder**: `src/proposal_check/`
- **Files you may change**: `src/proposal_check/test_proposal_check.py`
- **Signature under test**: `proposal_check(item: dict) -> list[str]`
- **Inputs**: as stated. Import with `from proposal_check.proposal_check import proposal_check`.
- **Outputs**: list of rule names broken, in this fixed order, empty if none: 'not_proposal' (kind != 'proposal'), 'no_page' (no line starting 'page: ' with a non-empty value before the '---' line), 'no_target' (no 'target: ' line), 'bad_target' (target does not start with 'roles/' or 'policy/', or contains '..'), 'no_diff' (nothing after the first line that is exactly '---'), 'multi_file' (the diff has a number of lines starting with '+++ ' other than exactly one), 'target_mismatch' (the single '+++ ' line's path, after stripping a leading 'b/', is not equal to target).
- **Errors**: none. A missing sheet key is treated as an empty sheet.
- **Allowed imports**: pytest and the function under test. Nothing else. No record server is needed.
- **Checklist items this job serves**: Story 0002-4 items 1, 2, 3, 4
- **Setup**: hand-build item dicts. A helper that returns a dict is fine; sheet text is a plain multi-line string in the shape of roles/shared/proposal-format.md.
- **Cases**, one test function each, exactly these names, nothing more:
  - `test_two_files_rejected`: sheet with page and target roles/x.md and a diff containing two '+++ ' lines -> ['multi_file'] (plus 'target_mismatch' only if the first path differs; assert 'multi_file' in result)
  - `test_src_target_rejected`: target src/a/a.py -> 'bad_target' in result
  - `test_no_page_rejected`: header without a page line -> 'no_page' in result
  - `test_clean_passes`: page p1, target roles/builder/SKILL.md, care low, diff with exactly '--- a/roles/builder/SKILL.md' and '+++ b/roles/builder/SKILL.md' lines and one changed line -> []
  - `test_wrong_kind_rejected`: kind 'story' with a clean sheet -> ['not_proposal']
- **Checks to run before reporting**:
  - `python -m pytest src/proposal_check -q`
  - `wc -l src/proposal_check/test_proposal_check.py   (must print 50 or less)`
- **Out of scope**: the code file, any other folder, any case not listed, any assertion on internals.
