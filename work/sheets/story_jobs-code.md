# Instruction sheet

- **Job id**: 0003-1-story_jobs-code
- **Kind**: code
- **Parent Story**: 0003-1
- **Function name**: `story_jobs`
- **Folder**: `src/story_jobs/`
- **Files you may change**: `src/story_jobs/story_jobs.py`, `src/story_jobs/story_jobs.md`
- **Signature**: `story_jobs(story_id: str, graph: dict) -> dict`
- **Inputs**: story_id in graph; graph is the dict documented in work/sheets/record_graph-code.md.
- **Outputs**: {'has_intent': bool (walking parent from the story reaches an item of kind intent), 'code': sorted ids of kind code whose parent is story_id, 'tests': dict code_id -> sorted ids of kind test whose checks contains that code_id, with [] when none}
- **Errors**: ValueError if story_id not in graph.
- **Allowed imports**: none. Nothing else.
- **Checklist items this job serves**: Story 0003-1 items 1, 5
- **How**: Pure. Walk parent with a visited set. Under 35 lines.
- **Checks to run before reporting**:
  - `ruff format src/story_jobs` then `ruff check src/story_jobs` (both clean; a noqa comment fails the shape check)
  - `python tools/lint.py`   (no findings for your folder)
  - `python -c "import ast,sys; t=ast.parse(open('src/story_jobs/story_jobs.py').read()); print(sum(isinstance(x,ast.FunctionDef) and not x.name.startswith('_') for x in t.body))"`   (must print 1)
- **Note file** `src/story_jobs/story_jobs.md` has exactly these six lines: purpose, signature, inputs, outputs, side effects, work item id 0003-1-story_jobs-code.
- **Out of scope**: tests, any other folder, any import not listed. Never hand-pack lines. If the formatted file exceeds 50 lines, report blocked.
