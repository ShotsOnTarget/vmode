# Instruction sheet

- **Job id**: 0002-2-wiki_stale-test
- **Kind**: test
- **Parent Story**: 0002-2
- **Function name**: `wiki_stale`
- **Folder**: `src/wiki_stale/`
- **Files you may change**: `src/wiki_stale/test_wiki_stale.py`
- **Signature under test**: `wiki_stale(root: str, days: int, now: str) -> list[str]`
- **Inputs**: as stated. Import with `from wiki_stale.wiki_stale import wiki_stale`.
- **Outputs**: sorted list of page ids whose last_used is more than `days` days before now. Empty root or missing root returns [].
- **Errors**: ValueError if a page's last_used is not ISO-8601.
- **Allowed imports**: pytest, json, os, datetime, and the function under test plus wiki_write/wiki_read where the case needs a page on disk. Nothing else. No record server is needed for these tests.
- **Checklist items this job serves**: Story 0002-2 items 4
- **Setup**: use `tmp_path` as root or log path. For wiki pages you may write files directly in the documented format (see work/sheets/wiki_write-code.md How line) or via wiki_write.
- **Cases**, one test function each, exactly these names, nothing more:
  - `test_old_page_listed`: page last_used 40 days before now, days=30 -> [id]
  - `test_fresh_page_not_listed`: page last_used 5 days before now, days=30 -> []
  - `test_readme_skipped`: a README.md in root does not raise and is not listed
  - `test_missing_root_empty`: nonexistent root -> []
- **Checks to run before reporting**:
  - `python -m pytest src/wiki_stale -q`
- **Out of scope**: the code file, any other folder, any case not listed, any assertion on internals.
