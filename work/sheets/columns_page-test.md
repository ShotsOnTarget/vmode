# Instruction sheet

- **Job id**: 0001-6-columns_page-test
- **Kind**: test
- **Parent Story**: 0001-6
- **Function name**: `columns_page`
- **Folder**: `src/columns_page/`
- **Files you may change**: `src/columns_page/test_columns_page.py` and nothing else. You never create or edit the code file.
- **Signature under test**: `columns_page() -> str`
- **Inputs**: as stated. Import with `from columns_page.columns_page import columns_page`.
- **Outputs**: an HTML document string
- **Errors**: none
- **Allowed imports**: pytest, tomllib, board_config, and the function under test. Nothing else.
- **Checklist items this job serves**: Story 0001-6 items 3
- **Setup**: none needed.
- **Cases**, one test function each, exactly these names, nothing more:
  - `test_is_html`: starts with '<!doctype html>' case-insensitive and contains '</html>'
  - `test_has_endpoint`: contains '/api/columns'
  - `test_no_external`: does not contain 'http://' or 'https://'
  - `test_side_by_side`: contains 'display:flex' or 'display: flex'
  - `test_no_overflow_rules`: contains 'text-overflow' and 'word-break' and 'overflow-x'
  - `test_no_dialogs`: contains none of 'alert(', 'confirm(', 'prompt('
- **Checks to run before reporting**:
  - `ruff format src/columns_page` then `ruff check src/columns_page` (both clean)
  - `python -m pytest src/columns_page -q`
- **Out of scope**: the code file, any other folder, any case not listed.
